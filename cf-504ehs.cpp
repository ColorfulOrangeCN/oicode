#include <bits/stdc++.h>
using namespace std;
constexpr int lgn = 20, maxn = 3e5 + 4, inf = 0x3f3f3f3f;

typedef unsigned long HashV;
typedef pair<HashV, HashV> HashType;
inline HashType operator+(HashType a, HashType b) {return HashType(a.first + b.first, a.second + b.second);}
inline HashType operator*(HashType a, HashType b) {return HashType(a.first * b.first, a.second * b.second);}
inline HashType operator-(HashType a, HashType b) {return HashType(a.first - b.first, a.second - b.second);}



int main() {
  
  return 0;
}
